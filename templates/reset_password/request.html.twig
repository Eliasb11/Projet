{% extends 'base.html.twig' %}

{% block title %}Reinitialisation MDP{% endblock %}

{% block body %}
    {% for flashError in app.flashes('reset_password_error') %}
        <div class="alert alert-danger" role="alert">{{ flashError }}</div>
    {% endfor %}
    <h1 style="text-align: center">Reinitialiser le mot de passe</h1>
    <div style="text-align: center">
        {{ form_start(requestForm) }}
        {{ form_row(requestForm.email) }}

        <div style="text-align: center">
            <small>
                Entrez votre Adresse Mail, un lien vous sera envoyer pour réinitliser votre mot de passe.
            </small>
        </div>

        <button class="btn btn-primary">Envoyer le Mail</button>
        {{ form_end(requestForm) }}
    </div>
{% endblock %}